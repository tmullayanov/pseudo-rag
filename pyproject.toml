[project]
name = "simple-rag"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "groq>=0.18.0",
]

[dependency-groups]
dev = [
    "pytest>=8.3.4",
]
